fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
pydantic>=2.7
python-multipart>=0.0.9
aiofiles>=23.0
python-docx>=1.1
pymupdf>=1.24
openpyxl>=3.1
anthropic>=0.40
openai>=1.50
keyring>=25.0
# Milestone 5 — media processing: bundled ffmpeg + local speech-to-text.
# Parakeet (preferred ASR) only ships for Apple Silicon; faster-whisper is the fallback everywhere.
imageio-ffmpeg>=0.5
faster-whisper>=1.0
parakeet-mlx ; sys_platform == "darwin" and platform_machine == "arm64"
pytest>=8.0
httpx>=0.27
# Build-only (Milestone 3 desktop packaging): bundles the backend for the Electron app
pyinstaller>=6.0
